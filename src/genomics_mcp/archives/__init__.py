"""Archive sources (E6): EGA and ENA clients.

MCP provider registration (`register(registry)`) is added during core integration; until then
this package must not be merged into a branch whose registry loads `genomics_mcp.archives`.
"""
